// && -> AND Gate
// || -> OR Gate

let a = true;
let b = false;

console.log(a && b);    // Output: false (AND gate, both must be true)
console.log(a || b);    // Output: true (OR gate, at least one must be true)    
console.log(!a);       // Output: false (NOT gate, negates the value)